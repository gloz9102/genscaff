const form=document.querySelector('#visit-form');
const date=form.elements.date;const now=new Date();date.min=[now.getFullYear(),String(now.getMonth()+1).padStart(2,'0'),String(now.getDate()).padStart(2,'0')].join('-');
form.addEventListener('submit',event=>{event.preventDefault();const result=document.querySelector('#visit-result');result.replaceChildren();const title=document.createElement('strong');title.textContent=`${form.elements.name.value.trim()}님의 방문 일정 미리보기`;const detail=document.createElement('p');detail.textContent=`${date.value} ${form.elements.time.value} / ${form.elements.space.value}`;const note=document.createElement('p');note.textContent='데모 확인이 완료되었습니다. 실제 예약은 접수되지 않습니다.';result.append(title,detail,note);result.hidden=false;result.scrollIntoView({block:'nearest'});});
