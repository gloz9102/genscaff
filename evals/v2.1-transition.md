# v2.1 source transition — 2026-09-17

Version 2.1.0 removes legacy distribution and invocation compatibility. This is a local source transition, not a published release or a behavioral acceptance decision. Commits, pushes, tags, and deployment were not performed.

## Implementation and structural verification

- Removed the 31 tracked files under `skill/genscaff`, legacy ZIP production, and `--kind legacy`. `all` produces the plugin and evaluation archives; evaluation filenames use the manifest version. Historical v2.0.0 summaries remain unchanged.
- Retired `$genscaff strict` without automatic Standard fallback or audit execution. `$genscaff-release-audit` and supported report schemas remain available.
- Updated manifest, structure checks, CI installation paths, dependency-update paths, contribution guidance, and both READMEs.
- Preserved both UI craft files byte-for-byte: SHA-256 `c22edde6873df9086cc4288eb195fbd0f6aec7157e6ae965b4de7425e19d179c`. Their enforcement and open-direction routing condition remain unchanged.
- Structure validation, 26 repository tests, 18 Core tests, 87 Strict regression tests, and three Node syntax checks passed. Packaging tests extract the ZIP and execute both report initialization and safe validation rejection without repository imports or active-browser approval.
- The earlier Strict differential run compared 71 ordered error lists across 87 passing tests; its records remain in the historical bundle. These checks establish implementation behavior, not generated-design quality.

## Runtime recovery and evaluation status

CLI 0.145.0 failed native Windows patch writes. Explicitly selecting the app-bundled CLI 0.155.0-alpha.2.6 passed the write preflight and resumed generation with `workspace-write`, scoped workspace trust, and approval policy `never`. No global sandbox or trust settings were changed. Browser discovery and execution remained inconsistent across model runs; parent browser observations are recorded separately from agent verification claims.

All trials retained `gpt-5.6-terra` / `medium`, matched prompts, frozen skill inputs, and isolated workspaces.

| Batch | Attempted | Process success | Status |
| --- | ---: | ---: | --- |
| Initial PR | 16 | 9 | Seven failed after the provider reported exhausted workspace credits; four complete pairs exist |
| Separate behavioral cases | 12 | 12 | Observed separately; not a substitute for PR acceptance |
| Revised common-flow PR | 16 | 0 | Credits exhausted before completion |
| Revised no-browser pair | 2 | 0 | Credits exhausted before completion |

The provider returned `Your workspace is out of credits. Add credits to continue.` Further model retries stopped. Process success does not establish task acceptance. The 120-run release evaluation was not run. Required human review and any order-swapped judgment disagreements remain pending; no model judgment is recorded as human adjudication.

## Observed behavior

| Case | Previous instructions | Initial simplified instructions |
| --- | --- | --- |
| User selects a direction | Two candidates, actual wait, fixed B response, working booking confirmation | Same boundary and working confirmation |
| Single direction | No forced alternatives; booking confirmation works | Same |
| Browser prohibited | Honest unverified status, but incorrectly skipped candidate comparison under delegation | Retained two unverified descriptions before delegated selection; honest status |
| CSS preservation | Required monthly price and accessible Cancel subscription control restored; cancellation observed | Same |
| Defect after aesthetic passes | Date retained, Cancel closes dialog and restores focus; booking works | Same |
| Narrow established-system fix | Only capacity-label CSS changed; booking behavior retained | Same |

Parent probes at 1440×900 and 390×844 are retained outside Git. Earlier probes that assumed a select instead of radios or ISO-formatted visible dates were corrected; those probe failures are not product defects. The post-aesthetic fixture's pre-existing clipped capacity label was outside its requested defect scope.

The initial simplified PR coffee output skipped comparison by misreading delegated selection as a single-direction request. Its first purchase succeeded, but reconfiguration left the submit button disabled. The incidents output retained filters across row action and undo but not reload; the previous output retained them across reload too. Both inventory outputs completed replenishment and retained filters across reload. Both transfer outputs retained input during validation, disabled duplicate submission, displayed receipts, and returned to usable forms. These observations do not establish overall superiority or full acceptance.

The second instruction candidate clarifies the existing comparison/selection distinction without adding a design prohibition, report field, or score. Its required PR rerun did not complete. The applied instruction changes therefore remain **provisional and behaviorally unaccepted**. No claim of reduced token cost, improved design, or completed evaluation is made.

Across the six separate behavioral pairs, the median treatment-minus-control difference was +6.7185 seconds, +1.5 completed tool calls, +6,292 reported input tokens, and +526 reported output tokens. These are recorded trace counters, not billed costs; cached and reasoning counters remain separately available in the raw summary. The smaller instruction file did not establish lower execution cost in this sample.

## Artifacts and remaining work

Raw artifacts are in the sibling checkout-external bundle `genscaff-evaluation/20260917-v21-resume`: `pr-v21`, `behavior-v21`, their `revision-2` directories, `parent-review`, `anonymous-review`, immutable migration candidates, pre-migration backups, and the retired legacy snapshot. The machine-readable `v2.1-transition.json` identifies hashes and counts. The earlier `20260917-v21` bundle and [historical summary](instruction-refactor.md) remain preserved.

Credits were restored and the revised PR batch completed all 16 processes in fresh `pr-v21-revision-2-funded` workspaces. The affected no-browser pair also completed both processes in `behavior-v21-revision-2-funded`. No further patch-write or credit failures occurred. All sixteen PR outputs were rendered at 1440×900 and 390×844 and their primary flows were independently exercised. Parent observations do not upgrade the agents' own verification status.

## Completed rerun and acceptance limits

All eight pairs received blind and order-swapped model judgments. Seven pair preferences agreed; the booking preference disagreed. The user reviewed the sampled booking and transfer comparisons and selected the original right booking (control) and original left transfer (treatment). This resolves the booking winner disagreement. Human records cover winner preference only: numeric scores and hard-gate ratings remain attributed to the model, not the user.

Final paired preferences are treatment 6, tie 0, control 2 (booking and logistics). These preferences incorporate instruction compliance and do not establish superior visual design. Forward model dimension counts, reported separately, are:

| Dimension | Treatment win | Tie | Treatment loss |
| --- | ---: | ---: | ---: |
| Product fit | 1 | 5 | 2 |
| Visual hierarchy | 1 | 3 | 4 |
| Responsive/accessibility | 1 | 7 | 0 |

The treatment retained candidate comparisons in the reviewed rerun; controls omitted comparison in six cases. Both no-browser arms accurately reported unverified rendering; treatment compared two described directions while control skipped comparison. The earlier coffee reset failure did not recur. Booking/transfer input preservation, submission locks, confirmation, and re-entry passed; inventory queries survived replenishment and reload; editorial section navigation and table content passed. Security and logistics CTA paths reached local results and return/edit states.

Residual defects prevent full behavioral acceptance: both incident and inventory pairs overflow the document at 390px. Incident treatment retains filters during action/undo but loses them on reload, unlike control. The brief did not explicitly require reload persistence, so this remains a disclosed product difference rather than an invented hard requirement. Model gate passes do not erase visible defects. The instruction changes remain provisional; further remediation and any required common-flow rerun must preserve these original results.

For the PR rerun, median treatment-minus-control differences were −16.1255 seconds, −1.5 completed tool calls, +17,498 reported input tokens, and −1,353.5 output tokens. These trace counters do not establish lower billed cost. The earlier separate six-pair behavioral metrics remain distinct. No 120-run evaluation was performed.

Raw evidence includes `funded-metrics.json`, `funded-paired-review.json`, `funded-parent-observations.json`, `funded-render-overview.json`, `funded-behavior-observations.json`, `human-preferences.json`, `human-review.html`, and the funded run/anonymous-review directories. The human preference adapters explicitly retain model score provenance. The earlier failed traces and model disagreements remain preserved. The v2.1 source/package transition is complete; behavioral acceptance and design/cost improvement claims are withheld.

The final harness reports `SCORED_PAIRS=8` and `EVAL_RUN_VALID`. These establish record consistency; they do not override the visible defects or establish behavioral acceptance. Selected raw-evidence hashes are embedded in `v2.1-transition.json`.
